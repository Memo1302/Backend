class ProductManager {

       constructor() { 
    }
    getProducts() {
    }
    addProducts(titel, description, price, thumnail, code, stock){
    }
    getProductById(id) {
    }
};

const pm = new ProductoManager ();

pm.addProducts();

console.log("from", pm.getProductById());
const product = pm.getProductById(5);

console.log("search", product);